// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  productLogoLarge: "assets/images/verify-logo.svg",
  // fbAppId: "730908317567405",
  fbAppId: "656141844964819",
  serverUrl: "https://aduaba.herokuapp.com/api/v1/",
  googleProvider:
    "664765146601-tamt0arfam3tcju3n5g0566q4dufj27q.apps.googleusercontent.com",
  publicKey: "pk_test_988076157778d2623b69aa3fdb476de63f057f6c",
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
